{
  "collectionName": "components_blocks_heroes",
  "info": {
    "displayName": "Hero Section",
    "icon": "picture",
    "description": "Sección hero principal con imagen de fondo"
  },
  "options": {},
  "attributes": {
    "variant": {
      "type": "enumeration",
      "enum": ["default", "video", "form"],
      "default": "default",
      "required": true
    },
    "title": {
      "type": "string",
      "required": true
    },
    "subtitle": {
      "type": "text"
    },
    "backgroundImage": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["images"]
    },
    "overlayEnabled": {
      "type": "boolean",
      "default": true
    },
    "overlayOpacity": {
      "type": "integer",
      "min": 0,
      "max": 100,
      "default": 50
    },
    "ctaEnabled": {
      "type": "boolean",
      "default": true
    },
    "ctaText": {
      "type": "string"
    },
    "ctaLink": {
      "type": "string"
    },
    "ctaStyle": {
      "type": "enumeration",
      "enum": ["primary", "secondary", "outline"],
      "default": "primary"
    },
    "height": {
      "type": "enumeration",
      "enum": ["full", "tall", "medium", "short"],
      "default": "full"
    },
    "textAlignment": {
      "type": "enumeration",
      "enum": ["left", "center", "right"],
      "default": "center"
    }
  }
}
